# Video reference media probe (offline)

Import this crate in Settings, then open the **video workbench**.

Use model `probe-offline` (or `probe-offline-reject` to exercise guest-side max-2-video refusal). No commercial Key.

This is the offline E2E entry for `video-reference-media-v1`: submit handles, query host-media video output.
